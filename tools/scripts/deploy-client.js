#!/usr/bin/env node
// Stub: client deployment script not yet implemented.
// Deployment is configured per-client via Vercel or Cloudflare Pages.
console.error(
  'The deploy-client script is not yet implemented.\n' +
    'To deploy a client site, push to the client branch and the CI/CD pipeline ' +
    'will trigger the appropriate deployment via Vercel or Cloudflare Pages.\n' +
    'Manual deployment: cd into the client directory and run the platform CLI directly.'
);
process.exit(1);
